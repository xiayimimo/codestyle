package com.tycho.codestyle.nine;
/**
 * 九-2【强制】 velocity 调用 POJO 类的属性时，直接使用属性名取值即可，模板引擎会自动按规范
调用 POJO 的 getXxx() ，如果是 boolean 基本数据类型变量 （boolean 命名不需要加 is 前
缀 ） ，会自动调用 isXxx() 方法。
 * @author shenjuntao
 * @date 2020年1月3日
 */
public class VelocityTip {

	//${name}
	//${exsited}
}
